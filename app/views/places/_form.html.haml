= simple_form_for(@place) do |f|
  = f.error_notification

  .form-inputs
    = f.input :name
    = f.input :category_id
    = f.input :desc
    = f.input :contact_id

  .form-actions
    = f.button :submit
